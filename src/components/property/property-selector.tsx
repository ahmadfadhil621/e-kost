"use client";

/**
 * PropertySelector — placeholder for UI redesign plan.
 * Spec: show when no property active; heading "Select Property", list of property cards;
 * tap card → setActivePropertyId(id), navigate to /.
 * See .cursor/plans/ui_redesign_to_ui-description_0f023f53.plan.md
 */
export function PropertySelector() {
  return null;
}
