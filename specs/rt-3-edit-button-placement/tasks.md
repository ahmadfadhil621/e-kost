# RT-3 — Edit Button Placement: Tasks

## Layer: UI (only layer affected)

- [ ] **T1** Write E2E tests for room detail edit button (inline placement, touch target, navigation)
- [ ] **T2** Write E2E tests for tenant detail edit button (inline placement, touch target, navigation)
- [ ] **T3** Validate E2E tests (structural + review checklist)
- [ ] **T4** Implement: room detail — move Edit button inline with `h2`, add Pencil icon, `size="sm" variant="ghost"`
- [ ] **T5** Implement: tenant detail — move Edit button inline with `h2`, add Pencil icon, `size="sm" variant="ghost"`
- [ ] **T6** Run new E2E specs locally (expect pass since it's a UX-only change)
- [ ] **T7** Run full Vitest suite (`npm run test:run`) — no regressions
- [ ] **T8** Run `npm run lint`
- [ ] **T9** Commit checkpoint
