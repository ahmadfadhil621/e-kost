# Tasks — Room Available Card Cleanup (issue #103)

## Implementation Tasks

- [x] Add Vitest test: available card does not render action buttons (in `room-card.test.tsx`)
- [x] Add Playwright E2E test: available card has no action section (in `e2e/rooms/`)
- [x] Run new E2E spec locally to confirm it fails (TDD red)
- [x] Implement: remove action section + unused props/imports from `room-card.tsx`
- [x] Run `npm run test:run` — all Vitest tests pass
- [x] Run `npm run lint` — no errors
- [x] Run new E2E spec locally — passes (TDD green)
- [x] Commit checkpoint
